# 工具掌握及受限题目说明

> 本文档基于 `tests/` 全套 pytest 测试结果（27/27 通过，准确率 100%）与 `core_v2/` 源码静态分析整理，明确 Sd0p-V3.5 的能力边界与已知受限场景。

---

## 一、测试准确率

| 指标 | 数值 |
|---|---|
| 测试套件 | `tests/` 下 22 个测试文件，27 个测试用例 |
| 通过 | 27 |
| 失败 | 0 |
| 错误 | 0 |
| 准确率 | **100%** |
| 执行时间 | 0.18s |

测试覆盖场景（按 `tests/fixtures/` 与测试文件名归纳）：

| 测试文件 | 验证场景 |
|---|---|
| `test_v2_e2e.py` | 极客大挑战 2019 PHP（私有属性 + wakeup 绕过 + 弱比较） |
| `test_ab_classes.py` | A/B 类跨类 POP 链 |
| `test_callback_rce.py` | 回调 RCE（call_user_func） |
| `test_call_pop_chain.py` | call_user_func POP 链 |
| `test_lock_class.py` | Lock 类引用绕过 |
| `test_magic_shell.php` | 魔术方法触发 shell |
| `test_native_class_direct.py` | 内置类直接利用（XXE + SSRF） |
| `test_pop_chain_wakeup.py` | POP 链 + wakeup 绕过 |
| `test_raw_injection.py` | 原始值注入（正常/未找到属性/长度不匹配警告） |
| `test_reference_bypass.py` | 引用绕过（R: 让两值相等） |
| `test_secret_class.py` | Secret 类 sha1 弱比较绕过 |
| `test_serialize_hooks.py` | `__serialize` / `__unserialize` 钩子 |
| `test_soapclient_chain.py` | SoapClient SSRF 链 |
| `test_soapclient_multilevel.py` | SoapClient 多级链 |
| `test_string_escape.py` | 字符串逃逸（类内） |
| `test_string_escape_global.py` | 全局字符串逃逸（长度增加/减少） |
| `test_string_escape_reduction.py` | 字符串逃逸（替换长度减少） |
| `test_array_string_escape.py` | 数组字符串逃逸（delta=0/非 0/正常数组误报防护） |
| `test_v3_fiber.py` | PHP 8.1+ Fiber 协程 payload |
| `test_v3_splfixedarray.py` | PHP 7.0–7.4 SplFixedArray 整数溢出 |
| `test_vuln_class.py` | Vuln 类基础反序列化 |

---

## 二、已掌握场景清单

### 2.1 PHP 魔术方法与 POP 链

- ✅ `__destruct` 触发链（最常见入口）
- ✅ `__wakeup` 绕过（CVE-2016-7124，属性数 +1 触发 PHP 跳过 wakeup）
- ✅ `__toString` 链式触发（echo / 字符串拼接触发）
- ✅ `__call` 触发（未定义方法调用）
- ✅ `__get` / `__set`（属性访问触发）
- ✅ 跨类多级 POP 链（[chain/pop_resolver.py](file:///d:/Projects/MyTools/Sd0p-v3.0/core_v2/chain/pop_resolver.py) 递归解析）

### 2.2 弱类型与比较绕过

- ✅ MD5 弱比较（`0e` 开头哈希碰撞，如 `QNKCDZO` 与 `240610708`）
- ✅ `==` 与 `===` 区分处理
- ✅ sha1 数组绕过（`test_secret_class.py`）

### 2.3 引用与属性可见性

- ✅ PHP 引用 `R:N;`（让两属性指向同一值，绕过 `===` 同值比较）
- ✅ 私有属性序列化前缀 `\0ClassName\0prop`
- ✅ 保护属性序列化前缀 `\0*\0prop`
- ✅ 动态属性赋值

### 2.4 内置类利用

- ✅ `SimpleXMLElement` XXE（`file://` 协议读文件）
- ✅ `SoapClient` SSRF（嵌套对象触发 `__call` → HTTP 请求）
- ✅ `SplFileObject` LFI
- ✅ `Error` / `Exception` XSS / SSRF

### 2.5 字符串逃逸

- ✅ `str_replace('long', 'short', ...)` 长度减少型逃逸（delta < 0）
- ✅ `str_replace('short', 'long', ...)` 长度增加型逃逸（delta > 0）
- ✅ 数组场景字符串逃逸（`serialize(['k' => $user_input, ...])`）
- ✅ delta = 0 识别与诚实提示（不强行生成假 payload）

### 2.6 自定义序列化与钩子

- ✅ `__serialize` / `__unserialize`（PHP 7.4+ 钩子）
- ✅ 原始值注入 `raw_injections`（手工覆盖某属性的序列化值）
- ✅ Session 格式互转（`php` / `php_serialize` / `php_binary`）

### 2.7 回调与动态调用

- ✅ `call_user_func` / `call_user_func_array` POP 链
- ✅ 数组回调 RCE（如 `[new System, 'exec']`）

### 2.8 高级策略（V3 实验性）

- ✅ `FiberStrategy`（PHP 8.1+ Fiber 协程，骨架 payload）
- ✅ `SplFixedArrayStrategy`（PHP 7.0–7.4 整数溢出，含自动/手动 fallback）

### 2.9 PHAR 反序列化

- ✅ PHAR 文件生成（Stub + Manifest + payload + metadata + SHA1 signature，写盘完整 `.phar` 归档）
- ✅ 文件上传触发 phar 协议反序列化

### 2.10 工具链能力

- ✅ 本地 PHP 沙箱执行（[sandbox/local.py](file:///d:/Projects/MyTools/Sd0p-v3.0/core_v2/sandbox/local.py) 写临时文件调 `php` 真实执行）
- ✅ Docker PHP 7.4 / 8.1 测试环境（[docker/test_runner.php](file:///d:/Projects/MyTools/Sd0p-v3.0/docker/test_runner.php) 接收 base64 payload 实际 `unserialize` 验证）
- ✅ HTTP 投递（GET / POST，`requests.Session`，headers / cookies / URL 编码）
- ✅ POP 链可视化（networkx + matplotlib 渲染调用图）

---

## 三、受限场景清单

### 3.1 序列化类型限制

- ⚠️ **多字节字符长度计算**：[serializer/php.py:75](file:///d:/Projects/MyTools/Sd0p-v3.0/core_v2/serializer/php.py) 用 Python `len(value)` 而非 UTF-8 字节长度。纯 ASCII 字符串正常，含中文等多字节字符时声明长度会偏小，PHP `unserialize` 可能失败
- ⚠️ **PHP 枚举 `E:`**：未显式支持枚举类型的直接序列化
- ⚠️ **自定义序列化 `C:`**：仅在 fallback 路径手工拼接，非通用机制
- ⚠️ **字符串值内 null bytes**：未特殊处理 `\x00` 在字符串内容中的影响

### 3.2 PHP 解析能力限制

- ⚠️ **PHP 8.1+ 新语法**：未确认完整支持 enum、命名参数、属性 attributes、union types、match expression、readonly 属性、first-class callable syntax
- ⚠️ **复杂表达式**：闭包、箭头函数、匿名类等解析深度有限

### 3.3 字符串逃逸算法限制

- ⚠️ **abs(delta) 与后续原结构 `";` 位置不整除**：经典字符串逃逸要求 `abs(delta) * k` 精确命中后续序列化结构中 `";` 的位置。当 abs(delta) 与位置不整除时（如 abs(delta)=6，`";` 位置 14），无法自动生成可解 payload，引擎会诚实返回"需手动计算"提示
- ⚠️ **delta = 0 的非宽字节场景**：UTF-8 下两字符串长度相同（如"危险"→"安全"各 6 字节）时无法自动生成，需手动用宽字节注入或其他 trick
- ⚠️ **多次 filter / 多次序列化**：当前仅处理单次 filter 单次序列化场景

### 3.4 高级策略完整性

- ⚠️ **FiberStrategy 为实验性骨架**：[strategy/advanced/fiber_strategy.py](file:///d:/Projects/MyTools/Sd0p-v3.0/core_v2/strategy/advanced/fiber_strategy.py) 仅生成实验性 payload，未完整覆盖所有 Fiber 反序列化变种
- ⚠️ **PHP-FPM / PHP 8.1 Enum 策略**：tests 显示有相关 fixture，但策略实现深度需按实际题目验证

### 3.5 AI 与沙箱限制

- ⚠️ **AI 建议非 LLM**：[ai/assistant.py](file:///d:/Projects/MyTools/Sd0p-v3.0/core_v2/ai/assistant.py) 是规则模板检测（危险魔术方法 / 危险函数 / MD5 弱比较 / 内置类利用 / 字符串逃逸），非真 LLM 推理。无法处理需要语义推理的复杂场景
- ⚠️ **沙箱依赖 PHP 可执行文件**：本地沙箱要求系统 `php` 命令可用，无 PHP 环境时无法实际验证 payload 行为

### 3.6 多文件与多类题目

- ⚠️ **多文件题目需手动拼接**：引擎接口为 `analyze_and_generate(php_code: str)`，多文件题目需调用方拼接所有 PHP 文件内容后喂入，无目录级自动合并
- ⚠️ **多入口类选择**：[engine.py:21](file:///d:/Projects/MyTools/Sd0p-v3.0/core_v2/engine.py) 取 `classes[0]` 作为入口，多入口场景可能选错

### 3.7 测试断言宽松性

- ⚠️ **断言多为子串包含**：测试用 `assert "O:4:\"Name\":3:{" in payload` 等子串断言，未完整 PHP `unserialize` 模拟校验。理论上存在"通过断言但实际 payload 无法 unserialize"的边角情况

---

## 四、能力边界速查表

| 题目类型 | 自动生成 | 备注 |
|---|---|---|
| 单类 `__destruct` + sink | ✅ | 标准场景 |
| `__wakeup` 绕过 | ✅ | CVE-2016-7124 |
| `__toString` 链 | ✅ | echo 触发 |
| `__call` 链 | ✅ | 未定义方法 |
| 跨类多级 POP | ✅ | 递归解析 |
| MD5 / sha1 弱比较 | ✅ | `0e` 碰撞 / 数组绕过 |
| 引用 `R:` 绕过 | ✅ | 同值 `===` |
| 私有 / 保护属性 | ✅ | `\0` 前缀 |
| SimpleXMLElement XXE | ✅ | `file://` 读 |
| SoapClient SSRF | ✅ | 嵌套对象 |
| SplFileObject LFI | ✅ | |
| 字符串逃逸（长度变化） | ✅ | delta ≠ 0 且可对齐 |
| 字符串逃逸（长度不变） | ⚠️ | 诚实提示，需手动 |
| 字符串逃逸（abs(delta) 不整除） | ⚠️ | 算法上无自动解 |
| `__serialize` / `__unserialize` | ✅ | PHP 7.4+ |
| 原始值注入 | ✅ | `raw_injections` |
| Session 格式转换 | ✅ | 三种格式互转 |
| 回调 RCE | ✅ | `call_user_func` |
| PHP 8.1 Fiber | ⚠️ | 实验性骨架 |
| SplFixedArray 溢出 | ✅ | PHP 7.0–7.4 |
| PHAR 反序列化 | ✅ | 生成完整 .phar |
| 多文件题目 | ⚠️ | 需手动拼接 |
| PHP 8.1 Enum 反序列化 | ⚠️ | 策略实现深度待验证 |
| 复杂语义推理 | ❌ | AI 模块为规则模板，非 LLM |

---

## 五、推荐使用方式

1. **优先喂入完整 PHP 源码**（多文件题目手动拼接所有 `.php` 内容）
2. **关注引擎输出提示**：当返回"需手动"类提示时，引擎已诚实识别场景但无法自动构造，应手动分析
3. **关键 payload 用沙箱验证**：[docker/test_runner.php](file:///d:/Projects/MyTools/Sd0p-v3.0/docker/test_runner.php) 或本地 `php` 命令实际 `unserialize` 验证
4. **含多字节字符时检查长度声明**：手动校验 `s:N:"..."` 中 N 等于字符串 UTF-8 字节长度
5. **高级策略（Fiber/SplFixedArray）需确认 PHP 版本匹配**

---

## 六、回归基线

`tests/` 全套 27 个用例通过率 100% 是当前基线。后续修改 `core_v2/` 任何文件后，应运行：

```bash
python -m pytest tests/ -v
```

确保通过率不下降，再发布版本。
